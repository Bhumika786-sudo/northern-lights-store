class CreateActiveAdminComments < ActiveRecord::Migration[7.1]
  def change
    # Table 'active_admin_comments' already exists.
    # We don't need to create it again, so this migration is empty.
  end
end
